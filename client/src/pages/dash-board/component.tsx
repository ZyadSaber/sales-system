import { memo } from "react";

const DashBoardPage = () => {

    return (
        <div>
            <h1>Dashboard</h1>
            {/* <button onClick={() => console.log(dd)}>dd</button> */}
        </div>
    );
};
export default memo(DashBoardPage);