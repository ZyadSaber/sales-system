const normalizeTableResponse = require("./normalizeTableResponse");
const normalizeList = require("./normalizeList");
const throwTheError = require("./throwTheError");

module.exports = {
  normalizeTableResponse,
  normalizeList,
  throwTheError,
};
