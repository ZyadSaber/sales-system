'use client'

import { memo } from "react";

const UsersPage = () => {

    return (
        <>
            <h1>users page</h1>
            {/* <button onClick={handle}>click me</button> */}
        </>
    )
}

export default memo(UsersPage)